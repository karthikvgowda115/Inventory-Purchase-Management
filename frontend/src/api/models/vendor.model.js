export default class Vendor {
  constructor({ id, name, contact, address, balance }) {
    this.id = id;
    this.name = name;
    this.contact = contact;
    this.address = address;
    this.balance = balance;
  }
}
